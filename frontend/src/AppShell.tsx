/**
 * UrbanCity application shell.
 *
 * V2 keeps the existing runtime implementation in AppShellLegacy while
 * progressively extracting features into dedicated components and hooks.
 */
export { AppShell } from "./AppShellLegacy";
