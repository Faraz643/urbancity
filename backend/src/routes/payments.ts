import { Router } from 'express';
import { z } from 'zod';
import crypto from 'crypto';
import { Webhook } from 'standardwebhooks';
import { prisma } from '../db';
import { authenticate, requireActiveUser, AuthRequest } from '../middleware/auth';

const router = Router();
const MAX_MINUTES = 48 * 60;
const API_VERSION = process.env.CASHFREE_API_VERSION || '2025-01-01';
const CHECKOUT_LOCK_MINUTES = 5;
const CHECKOUT_LOCK_MESSAGE = 'Someone else is booking this space right now. Please check another board or try again after 5 minutes.';
const BASE_CURRENCY = 'USD';

type P = { main_per30_usd:any; main_one_day_usd:any; wall_per30_usd:any; wall_one_day_usd:any; corner_per30_usd:any; corner_one_day_usd:any; };
async function getPricing(tx:any){const r=await tx.$queryRaw<P[]>`SELECT main_per30_usd, main_one_day_usd, wall_per30_usd, wall_one_day_usd, corner_per30_usd, corner_one_day_usd FROM pricing_settings WHERE id='default'`;if(!r[0])throw Object.assign(new Error('Pricing is not configured.'),{status:503});return r[0];}
function priceFor(p: P, type: string, minutes: number) {
  if (minutes <= 0 || minutes % 30 !== 0 || minutes > MAX_MINUTES) throw new Error('Duration must be in 30-minute steps, maximum 2 days');
  const t = String(type || '').trim().toLowerCase();
  const wall = t === 'wall' || t === 'building wall';
  const main = t === 'premium' || t === 'premium road' || t === 'vertical';
  const corner = !wall && !main;
  const per30 = Number(wall ? p.wall_per30_usd : corner ? p.corner_per30_usd : p.main_per30_usd);
  const day = Number(wall ? p.wall_one_day_usd : corner ? p.corner_one_day_usd : p.main_one_day_usd);
  if (minutes < 1440) return Number(((minutes / 30) * per30).toFixed(2));
  const days = Math.floor(minutes / 1440), rem = minutes % 1440;
  return Number((days * day + (rem / 30) * per30).toFixed(2));
}
function cashfreeBaseUrl(){return(process.env.CASHFREE_ENV||'sandbox').toLowerCase()==='production'?'https://api.cashfree.com/pg':'https://sandbox.cashfree.com/pg';}
function cashfreeHeaders(idempotencyKey?:string){const id=process.env.CASHFREE_CLIENT_ID,secret=process.env.CASHFREE_CLIENT_SECRET;if(!id||!secret)throw Object.assign(new Error('Cashfree Payments is not configured.'),{status:503});return{'Content-Type':'application/json','Accept':'application/json','x-api-version':API_VERSION,'x-client-id':id,'x-client-secret':secret,...(idempotencyKey?{'x-idempotency-key':idempotencyKey}:{})};}
function dodoBaseUrl(){return(process.env.DODO_PAYMENTS_ENVIRONMENT||'live_mode').toLowerCase()==='test_mode'?'https://test.dodopayments.com':'https://live.dodopayments.com';}
function dodoHeaders(){const key=process.env.DODO_PAYMENTS_API_KEY;if(!key)throw Object.assign(new Error('Dodo Payments is not configured. Add DODO_PAYMENTS_API_KEY.'),{status:503});return{'Content-Type':'application/json','Accept':'application/json','Authorization':'Bearer '+key};}
function providerError(status:number,body:any){return Object.assign(new Error(String(body?.message||body?.error||body?.type||'Payment provider request failed')),{status:status>=400&&status<600?status:502});}
async function country(req:AuthRequest){const h=String(req.headers['x-urban-country']||req.headers['cf-ipcountry']||req.headers['x-vercel-ip-country']||'').trim().toUpperCase();if(/^[A-Z]{2}$/.test(h))return h;const forced=String(process.env.PAYMENT_DEFAULT_COUNTRY||'').trim().toUpperCase();if(/^[A-Z]{2}$/.test(forced))return forced;return'US';}
router.get('/country',async(req:AuthRequest,res,next)=>{try{res.json({country:await country(req)});}catch(e){next(e);}});
async function fx(){const c=new AbortController(),t=setTimeout(()=>c.abort(),4000);try{const r=await fetch('https://api.frankfurter.app/latest?from=USD&to=INR',{signal:c.signal});const d:any=await r.json();const v=Number(d?.rates?.INR);if(!r.ok||!Number.isFinite(v)||v<=0)throw new Error('FX rate unavailable');return v;}finally{clearTimeout(t);}}
function amountsMatch(actual:any,expected:any){const a=Number(actual),e=Number(expected);return Number.isFinite(a)&&Number.isFinite(e)&&Math.abs(a-e)<0.011;}
async function cashfreeOrder(orderId:string){const r=await fetch(cashfreeBaseUrl()+'/orders/'+encodeURIComponent(orderId),{headers:cashfreeHeaders()});const o:any=await r.json().catch(()=>({}));if(!r.ok)throw providerError(r.status,o);return o;}
async function dodoCheckout(sessionId:string){const r=await fetch(dodoBaseUrl()+'/checkouts/'+encodeURIComponent(sessionId),{headers:dodoHeaders()});const s:any=await r.json().catch(()=>({}));if(!r.ok)throw providerError(r.status,s);if(s.id&&s.id!==sessionId)throw Object.assign(new Error('Dodo checkout session does not match this payment'),{status:502});return s;}
async function activate(id:string,providerId?:string|null,eventId?:string|null,charged?:number,currency?:string){await prisma.$transaction(async tx=>{const p=await tx.payment.findUnique({where:{id},include:{booking:true}});if(!p||p.status==='SUCCEEDED')return;if(eventId){const prior=await tx.payment.findUnique({where:{providerEventId:eventId}});if(prior&&prior.id!==id)throw Object.assign(new Error('Payment webhook event has already been processed.'),{status:409});}const now=new Date();const taken=await tx.booking.findFirst({where:{billboardId:p.booking.billboardId,status:'ACTIVE',endDate:{gt:now},id:{not:p.bookingId}}});if(taken)throw Object.assign(new Error('This advertising space was booked by another completed payment.'),{status:409});const end=new Date(now.getTime()+p.booking.durationMinutes*60000);await tx.payment.update({where:{id},data:{status:'SUCCEEDED',providerPaymentId:providerId||p.providerPaymentId,providerEventId:eventId||p.providerEventId,amount:charged??p.amount,currency:currency||p.currency}});await tx.booking.update({where:{id:p.bookingId},data:{status:'ACTIVE',startDate:now,endDate:end}});await tx.billboard.update({where:{id:p.booking.billboardId},data:{isAvailable:false,currentBid:p.booking.amount,currentBidderId:p.userId}});await tx.checkoutLock.deleteMany({where:{billboardId:p.booking.billboardId}});});}
async function fail(id:string,status:'FAILED'|'CANCELLED'){const p=await prisma.payment.findUnique({where:{id}});if(!p||p.status==='SUCCEEDED')return;await prisma.$transaction([prisma.payment.update({where:{id},data:{status}}),prisma.booking.update({where:{id:p.bookingId},data:{status:status==='FAILED'?'PAYMENT_FAILED':'PAYMENT_CANCELLED'}}),prisma.checkoutLock.deleteMany({where:{bookingId:p.bookingId}})]);}
router.post('/checkout',authenticate,requireActiveUser,async(req:AuthRequest,res,next)=>{try{const d=z.object({billboardId:z.string(),durationMinutes:z.number().int().min(30).max(MAX_MINUTES),companyName:z.string().min(2).max(80).optional(),description:z.string().max(500).optional(),advertisementId:z.string().optional(),customerPhone:z.string().optional()}).parse(req.body);if(d.durationMinutes%30!==0)return res.status(400).json({error:'Choose time in 30-minute steps'});
const c = await country(req);
const provider = 'DODO';
const now = new Date();
const customerPhone=String(d.customerPhone||'').replace(/\D/g,'');
const prepared=await prisma.$transaction(async tx=>{await tx.checkoutLock.deleteMany({where:{expiresAt:{lte:now}}});const lock=new Date(now.getTime()+CHECKOUT_LOCK_MINUTES*60000);let b=await tx.billboard.findUnique({where:{id:d.billboardId}});const pricing=await getPricing(tx);if(!b){const wall=d.billboardId.startsWith('W');b=await tx.billboard.create({data:{id:d.billboardId,name:(wall?'Wallboard ':'Billboard ')+d.billboardId,type:wall?'Wall':'Premium Road',positionX:0,positionY:0,positionZ:0,location:'UrbanCity',isAvailable:true,isActive:true,minBid:wall?pricing.wall_per30_usd:pricing.main_per30_usd}});}if(!b)throw Object.assign(new Error('Billboard not found'),{status:404});if(await tx.booking.findFirst({where:{billboardId:d.billboardId,status:'ACTIVE',endDate:{gt:now}}}))throw Object.assign(new Error('This advertising space is currently reserved or booked'),{status:409});const current=await tx.checkoutLock.findUnique({where:{billboardId:d.billboardId}});if(current&&current.userId!==req.user!.id)throw Object.assign(new Error(CHECKOUT_LOCK_MESSAGE),{status:409});const amount=priceFor(pricing,b.type,d.durationMinutes);let booking:any;if(current?.bookingId){const old=await tx.booking.findFirst({where:{id:current.bookingId,userId:req.user!.id,status:'PAYMENT_PENDING'},include:{payment:true}});if(old){booking=await tx.booking.update({where:{id:old.id},data:{startDate:now,endDate:lock,durationMinutes:d.durationMinutes,amount,companyName:d.companyName||req.user!.displayName||req.user!.username,description:d.description||null,advertisementId:d.advertisementId}});if(old.payment)await tx.payment.delete({where:{id:old.payment.id}});}}if(!booking)booking=await tx.booking.create({data:{userId:req.user!.id,billboardId:d.billboardId,startDate:now,endDate:lock,durationMinutes:d.durationMinutes,amount,companyName:d.companyName||req.user!.displayName||req.user!.username,description:d.description||null,advertisementId:d.advertisementId,status:'PAYMENT_PENDING'}});if(current)await tx.checkoutLock.update({where:{id:current.id},data:{userId:req.user!.id,bookingId:booking.id,expiresAt:lock}});else await tx.checkoutLock.create({data:{billboardId:d.billboardId,userId:req.user!.id,bookingId:booking.id,expiresAt:lock}});const paymentRow=await tx.payment.create({data:{bookingId:booking.id,userId:req.user!.id,provider,amount,currency:BASE_CURRENCY,status:'PENDING'}});return{booking,payment:paymentRow,amount,provider,country:c};});try{const frontend=process.env.FRONTEND_URL||'http://localhost:5173';if(prepared.provider==='CASHFREE'){const rate=await fx(),inr=Math.round(prepared.amount*rate*100)/100,env=(process.env.CASHFREE_ENV||'sandbox').toLowerCase(),phone=customerPhone;const orderId='UC_'+prepared.payment.id.replace(/-/g,'').slice(0,40);const r=await fetch(cashfreeBaseUrl()+'/orders',{method:'POST',headers:cashfreeHeaders(crypto.randomUUID()),body:JSON.stringify({order_id:orderId,order_amount:Number(inr.toFixed(2)),order_currency:'INR',customer_details:{customer_id:req.user!.id,customer_name:req.user!.displayName||req.user!.username,customer_email:req.user!.email,customer_phone:phone},order_meta:{return_url:frontend+'/?payment=return&booking='+encodeURIComponent(prepared.booking.id)+'&order_id={order_id}',...(process.env.CASHFREE_NOTIFY_URL?{notify_url:process.env.CASHFREE_NOTIFY_URL}:{})},order_tags:{booking_id:prepared.booking.id,payment_id:prepared.payment.id,base_amount_usd:prepared.amount.toFixed(2),fx_rate_usd_inr:rate.toFixed(8)}})});const o:any=await r.json().catch(()=>({}));if(!r.ok)throw providerError(r.status,o);if(!o.payment_session_id||!o.order_id)throw Object.assign(new Error('Cashfree did not return a payment session.'),{status:502});await prisma.payment.update({where:{id:prepared.payment.id},data:{providerPaymentId:o.order_id,checkoutSessionId:o.payment_session_id,status:'CHECKOUT_CREATED',amount:inr,currency:'INR'}});return res.status(201).json({bookingId:prepared.booking.id,paymentId:prepared.payment.id,orderId:o.order_id,paymentSessionId:o.payment_session_id,paymentProvider:'CASHFREE',amount:prepared.amount,currency:'USD',chargedAmount:inr,chargedCurrency:'INR',fxRate:rate,country:prepared.country,environment:env==='production'?'production':'sandbox'});}const product=process.env.DODO_PAYMENTS_PRODUCT_ID;if(!product)throw Object.assign(new Error('Dodo Payments is not configured. Add DODO_PAYMENTS_PRODUCT_ID.'),{status:503});const r=await fetch(dodoBaseUrl()+'/checkouts',{method:'POST',headers:dodoHeaders(),body:JSON.stringify({product_cart:[{product_id:product,quantity:1,amount:Math.round(prepared.amount*100)}],customer:{email:req.user!.email,name:req.user!.displayName||req.user!.username},billing_address:{country:prepared.country},return_url:frontend+'/?payment=return&booking='+encodeURIComponent(prepared.booking.id),cancel_url:frontend+'/?payment=return&booking='+encodeURIComponent(prepared.booking.id),metadata:{booking_id:prepared.booking.id,payment_id:prepared.payment.id}})});const s:any=await r.json().catch(()=>({}));if(!r.ok)throw providerError(r.status,s);if(!s.session_id||!s.checkout_url)throw Object.assign(new Error('Dodo did not return a checkout URL.'),{status:502});await prisma.payment.update({where:{id:prepared.payment.id},data:{providerPaymentId:s.session_id,checkoutSessionId:s.session_id,status:'CHECKOUT_CREATED',amount:prepared.amount,currency:'USD'}});return res.status(201).json({bookingId:prepared.booking.id,paymentId:prepared.payment.id,orderId:s.session_id,paymentSessionId:'DODO_URL:'+Buffer.from(s.checkout_url).toString('base64url'),checkoutUrl:s.checkout_url,paymentProvider:'DODO',amount:prepared.amount,currency:'USD',country:prepared.country,environment:process.env.DODO_PAYMENTS_ENVIRONMENT||'live_mode'});}catch(e){await fail(prepared.payment.id,'FAILED');throw e;}}catch(e){next(e);}});
router.get('/:bookingId/verify',authenticate,async(req:AuthRequest,res,next)=>{try{const orderId=String(req.query.order_id||'');const p=await prisma.payment.findFirst({where:{bookingId:req.params.bookingId,userId:req.user!.id},include:{booking:true}});if(!p)return res.status(404).json({error:'Payment not found'});if(p.status==='SUCCEEDED')return res.json({paid:true,paymentStatus:'SUCCEEDED',bookingStatus:'ACTIVE',amount:Number(p.booking.amount),currency:'USD',chargedAmount:Number(p.amount),chargedCurrency:p.currency});if(p.provider==='CASHFREE'){if(!orderId||orderId!==p.providerPaymentId)return res.status(400).json({error:'Payment order does not match this booking'});const o=await cashfreeOrder(orderId);if(o.order_status==='PAID'&&amountsMatch(o.order_amount,p.amount)&&o.order_currency==='INR'){await activate(p.id,orderId,undefined,Number(p.amount),'INR');return res.json({paid:true,paymentStatus:'SUCCEEDED',bookingStatus:'ACTIVE',amount:Number(p.booking.amount),currency:'USD',chargedAmount:Number(p.amount),chargedCurrency:'INR'});}if(['EXPIRED','TERMINATED'].includes(o.order_status))await fail(p.id,'CANCELLED');return res.json({paid:false,paymentStatus:p.status,bookingStatus:p.booking.status,providerStatus:o.order_status||'UNKNOWN',amount:Number(p.booking.amount),currency:'USD',chargedAmount:Number(p.amount),chargedCurrency:'INR'});}const checkoutSessionId=p.checkoutSessionId||p.providerPaymentId;if(!checkoutSessionId)return res.status(400).json({error:'Dodo checkout session is missing for this payment'});const s=await dodoCheckout(checkoutSessionId);const providerStatus=String(s.payment_status||s.status||s.payment?.status||'').toLowerCase();const providerAmount=Number(s.total_amount??s.amount??s.payment?.total_amount)/100;const providerCurrency=String(s.currency||s.payment?.currency||'').toUpperCase();if(providerStatus==='succeeded'&&amountsMatch(providerAmount,p.amount)&&providerCurrency==='USD'){await activate(p.id,s.payment_id||s.payment?.payment_id||p.providerPaymentId);return res.json({paid:true,paymentStatus:'SUCCEEDED',bookingStatus:'ACTIVE',providerStatus,amount:Number(p.booking.amount),currency:'USD',chargedAmount:Number(p.amount),chargedCurrency:'USD'});}if(providerStatus==='failed')await fail(p.id,'FAILED');if(providerStatus==='cancelled')await fail(p.id,'CANCELLED');return res.json({paid:false,paymentStatus:p.status,bookingStatus:p.booking.status,providerStatus:providerStatus||'UNKNOWN',amount:Number(p.booking.amount),currency:'USD',chargedAmount:Number(p.amount),chargedCurrency:'USD'});}catch(e){next(e);}});
router.post('/webhook/cashfree',async(req,res)=>{try{const raw=(req as any).rawBody,sig=String(req.headers['x-webhook-signature']||''),ts=String(req.headers['x-webhook-timestamp']||'');if(!raw||!sig||!ts)return res.status(400).json({error:'Missing webhook signature'});const secret=process.env.CASHFREE_CLIENT_SECRET;if(!secret)return res.status(503).json({error:'Cashfree is not configured'});const expected=crypto.createHmac('sha256',secret).update(ts+raw).digest('base64');if(!crypto.timingSafeEqual(Buffer.from(expected),Buffer.from(sig)))return res.status(401).json({error:'Invalid webhook signature'});const e:any=JSON.parse(raw),id=e?.data?.order?.order_id;const pay=await prisma.payment.findFirst({where:{provider:'CASHFREE',providerPaymentId:id},include:{booking:true}});if(!pay)return res.status(200).json({received:true});if(e?.data?.payment?.payment_status!=='SUCCESS')return res.status(200).json({received:true});const o=await cashfreeOrder(id);if(o.order_status!=='PAID'||o.order_currency!=='INR'||!amountsMatch(o.order_amount,pay.amount))return res.status(400).json({error:'Cashfree payment verification failed'});const eventId=String(req.headers['x-webhook-id']||e?.data?.payment?.cf_payment_id||'cashfree:'+id+':SUCCESS');const prior=await prisma.payment.findUnique({where:{providerEventId:eventId}});if(prior)return res.status(200).json({received:true,duplicate:true});await activate(pay.id,id,eventId,Number(o.order_amount),'INR');return res.status(200).json({received:true});}catch(e){return res.status(500).json({error:'Webhook processing failed'});}});
router.post('/webhook/dodo',async(req,res)=>{try{const secret=process.env.DODO_PAYMENTS_WEBHOOK_KEY;if(!secret)return res.status(503).json({error:'Dodo webhook secret is not configured'});const w=new Webhook(secret),raw=(req as any).rawBody||JSON.stringify(req.body);const webhookId=String(req.headers['webhook-id']||'');await w.verify(raw,{'webhook-id':webhookId,'webhook-signature':String(req.headers['webhook-signature']||''),'webhook-timestamp':String(req.headers['webhook-timestamp']||'')});const e:any=JSON.parse(raw),d:any=e?.data||{},m:any=d?.metadata||{};if(e.type!=='payment.succeeded')return res.status(200).json({received:true});const internalPaymentId=String(m.payment_id||'');const checkoutSessionId=String(d.checkout_session_id||'');let p:any=internalPaymentId?await prisma.payment.findUnique({where:{id:internalPaymentId},include:{booking:true}}):null;if(!p&&checkoutSessionId)p=await prisma.payment.findFirst({where:{provider:'DODO',checkoutSessionId},include:{booking:true}});if(!p)return res.status(200).json({received:true});const prior=webhookId?await prisma.payment.findUnique({where:{providerEventId:webhookId}}):null;if(prior)return res.status(200).json({received:true,duplicate:true});const providerStatus=String(d.status||'').toLowerCase();const providerAmount=Number(d.total_amount)/100;const providerCurrency=String(d.currency||'').toUpperCase();if(providerStatus!=='succeeded'||providerCurrency!=='USD'||!amountsMatch(providerAmount,p.amount))return res.status(400).json({error:'Dodo payment verification failed'});await activate(p.id,String(d.payment_id||p.providerPaymentId||''),webhookId||null,providerAmount,providerCurrency);return res.status(200).json({received:true});}catch(e){return res.status(400).json({error:'Invalid Dodo webhook'});}});
export { router as paymentRouter };
